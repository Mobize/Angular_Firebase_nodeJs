// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyA1Q3TDmwr83w6MjOD3WAx5KwQj9ZsClSE',
    authDomain: 'authentification-registres.firebaseapp.com',
    databaseURL: 'https://authentification-registres.firebaseio.com',
    projectId: 'authentification-registres',
    storageBucket: 'authentification-registres.appspot.com',
    messagingSenderId: '387619868267'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
